package sap.ui.layout;

@:native("sap.ui.layout.GridPosition")
@:enum extern abstract GridPosition(String)
{
    /**
    * <code>Grid</code> is centered on the screen.
    */
    var Center= "Center";
    /**
    * <code>Grid</code> is aligned left.
    */
    var Left= "Left";
    /**
    * <code>Grid</code> is aligned to the right.
    */
    var Right= "Right";
}
