package sap.m;

@:native("sap.m.LabelDesign")
@:enum extern abstract LabelDesign(String)
{
    /**
    * Displays the label in bold.
    */
    var Bold= "Bold";
    /**
    * Displays the label in normal mode.
    */
    var Standard= "Standard";
}
