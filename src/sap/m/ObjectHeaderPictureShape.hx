package sap.m;

@:native("sap.m.ObjectHeaderPictureShape")
@:enum extern abstract ObjectHeaderPictureShape(String)
{
    /**
    * Circle shape for the images in the <code>ObjectHeader</code>.
    */
    var Circle= "Circle";
    /**
    * Square shape for the images in the <code>ObjectHeader</code>.
    */
    var Square= "Square";
}
