package sap.m;

@:native("sap.m.SelectType")
@:enum extern abstract SelectType(String)
{
    /**
    * Will show the text.
    */
    var Default= "Default";
    /**
    * Will show only the specified icon.
    */
    var IconOnly= "IconOnly";
}
